services:
  app:
    build:
      context: .
      dockerfile: Dockerfile
      no_cache: true
    ports:
      - "5173:80"
    restart: unless-stopped

  docs:
    build:
      context: ./docs
      dockerfile: Dockerfile
      no_cache: true
    ports:
      - "4173:80"
    restart: unless-stopped
